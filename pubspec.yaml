name: pinyin
description: Convert Chinese characters to pinyin, simplified or traditional Chinese. Accurate and complete Chinese character dictionary. Fast conversion.
version: 3.2.0
homepage: https://github.com/sun-jiao/dart-pinyin

environment:
  sdk: ">=2.12.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  csv: ^5.1.1
  dio: ^5.4.0
  html: ^0.15.4
  lints: ^2.1.0
  test: ^1.24.0